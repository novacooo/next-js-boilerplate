import styles from './styles.module.scss';

const Home = () => (
  <div className={styles.container}>
    <main className={styles.main}>
      <p>Home</p>
    </main>
  </div>
);

export default Home;
